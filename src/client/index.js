// import handlebars from 'handlebars';

// const template = handlebars.compile('<h1>Hello, {{name}}!</h1>');
// const data = { name: 'World and others' };

// document.getElementById('app').innerHTML = template(data);
